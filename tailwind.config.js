// Configuração de Design do Klinni IA
module.exports = {
  theme: {
    extend: {
      colors: {
        // As cores que você escolheu para o padrão Boutique
        'ice-gray': '#F4F7F6',    // Fundo Relaxante
        'soft-blue': '#7FA9D1',   // Botão Principal (Início do Degradê)
        'deep-blue': '#9BB8CD',   // Botão Principal (Fim do Degradê)
        'clinic-white': '#FFFFFF' // Cards e Superfícies
      },
      borderRadius: {
        'clinic': '16px', // Bordas suaves que você solicitou
      },
      fontFamily: {
        'inter': ['Inter', 'sans-serif'], // Fonte moderna e limpa
      }
    }
  }
}
